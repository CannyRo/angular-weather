import { BackgroundIsDayOrNightDirective } from './background-is-day-or-night.directive';

describe('BackgroundIsDayOrNightDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundIsDayOrNightDirective();
    expect(directive).toBeTruthy();
  });
});

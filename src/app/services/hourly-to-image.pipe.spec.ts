import { HourlyToImagePipe } from './hourly-to-image.pipe';

describe('HourlyToImagePipe', () => {
  it('create an instance', () => {
    const pipe = new HourlyToImagePipe();
    expect(pipe).toBeTruthy();
  });
});

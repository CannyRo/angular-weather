import { DailyToImagePipe } from './daily-to-image.pipe';

describe('DailyToImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DailyToImagePipe();
    expect(pipe).toBeTruthy();
  });
});

import { CompassToDirectionPipe } from './compass-to-direction.pipe';

describe('CompassToDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new CompassToDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
